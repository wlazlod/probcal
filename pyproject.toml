[project]
name = "probcal"
version = "0.3.0"
description = "Universal post-hoc probability calibration for binary classifiers: methods, metrics, diagnostics, and auditable offsetting — numpy-only."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Daniel Wlazło" }]
keywords = [
    "calibration",
    "probability",
    "binary-classification",
    "credit-risk",
    "reliability",
    "isotonic-regression",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Operating System :: OS Independent",
]
dependencies = ["numpy>=1.26"]

[project.urls]
Homepage = "https://github.com/wlazlod/probcal"
Documentation = "https://wlazlod.github.io/probcal/"
Changelog = "https://github.com/wlazlod/probcal/blob/main/CHANGELOG.md"
Issues = "https://github.com/wlazlod/probcal/issues"

[project.optional-dependencies]
viz = ["matplotlib>=3.8"]
sklearn = ["scikit-learn>=1.4"]
optbinning = ["optbinning>=0.21"]
treecf = ["treecf>=0.2.4"]
bench = ["scikit-learn>=1.4", "netcal>=1.3.5", "betacal>=1.1.0", "pandas>=2"]
all = ["matplotlib>=3.8", "scikit-learn>=1.4", "optbinning>=0.21", "treecf>=0.2.4"]
dev = [
    "pytest>=8",
    "ruff>=0.4",
    "black>=24",
    "mypy>=1.10",
    "scipy>=1.11",          # reference for tests ONLY — never imported by src/probcal
    "scikit-learn>=1.4",    # reference for tests ONLY
    "statsmodels>=0.14",    # reference for tests ONLY
    "nbmake>=1.5",          # executes the end-to-end notebook in CI
]
docs = [
    "mkdocs-material>=9.5",
    "mkdocstrings[python]>=0.24",
    "mkdocs-jupyter>=0.24",
    "ipykernel>=6",  # executes the tutorial notebook (committed executed)
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/probcal"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.black]
line-length = 100

[tool.mypy]
python_version = "3.12"
packages = ["probcal"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: long-running test (bootstrap/CV-heavy)",
    "reference: compares against scipy/scikit-learn/statsmodels; skipped when those libs are absent",
]
